rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Registered students collection
    match /students/{studentId} {
      allow read, write: if true;
    }

    // Custom supplications collection
    match /duas/{duaId} {
      allow read, write: if true;
    }
  }
}
